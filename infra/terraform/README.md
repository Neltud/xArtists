# Terraform blueprint (non provisionné)

Référence EKS + RDS + Secrets Manager pour une éventuelle stack LIA prod.

**Ne pas appliquer** sans revue sécu, budget et secrets réels hors git.

Voir `lia_cluster.tf.example`.
