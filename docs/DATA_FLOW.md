# Flux d’intention

```
User → CommandBar / IntentBar
     → LIA-Brain (parser / optional LLM)
     → Guardian (policy)
     → Relayer (paper | live later)
     → Chain (MVX / ETH)
     → Feedback UI
```

Paiements : Payment Gateway route MoonPay (EGLD) vs Stripe (packs €).
