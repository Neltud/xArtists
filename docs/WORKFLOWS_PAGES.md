# GitHub Pages workflows

**Canonical (only auto deploy on push main):**  
`.github/workflows/static.yml` — *Deploy Vite app to GitHub Pages*

**Deprecated (manual only, no push trigger):**  
`pages.yml` · `jekyll-gh-pages.yml` · `deploy-frontend.yml` · `deploy-exclusive.yml` · `deploy-pages.yml`

Keep `ci-cd.yml` / `rust.yml` / etc. for non-Pages concerns.
