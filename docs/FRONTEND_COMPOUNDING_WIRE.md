# Wire CompoundingPanel into Trading.tsx

```tsx
import CompoundingPanel from '../components/CompoundingPanel'
// after <DeskPanel />:
<CompoundingPanel />
```

Optional route `/compounding` with same panel.
