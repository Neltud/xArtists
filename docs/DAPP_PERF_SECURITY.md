# dApp — performance & security

## Performance
- React.lazy all pages · TxShell only on TX routes
- Vite manualChunks (react/router/mx-sdk/virtual)
- Catalog index-first · VirtualNftGrid · LazyImage
- fetchJson timeout/abort · SW v4 network-first data
- esbuild drop console in CI

## Security
- LIA ops blocked as user wallet
- lib/security.ts safe URLs / explorer / redact
- Referrer-Policy + nosniff meta
- SC banners until codeHash · micro-proofs backend

## Residual
- GH Pages limited CSP headers
- Keep collections index published to avoid 274KB full load
