# DEPRECATED — use static.yml only (P0 2026-09-13)
# Previously could fight static.yml / push dist noise.
name: "[deprecated] deploy-pages"
on:
  workflow_dispatch:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Deprecated. Only static.yml should deploy the Vite app to Pages."
          exit 0
