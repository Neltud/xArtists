# DEPRECATED — use static.yml only (P0 2026-09-13)
name: "[deprecated] deploy-exclusive"
on:
  workflow_dispatch:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated. Canonical: static.yml"
