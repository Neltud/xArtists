# DISABLED auto-deploy — use workflow "Deploy xArtists Exclusive" only
name: Deploy GitHub Pages (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use Deploy xArtists Exclusive."
