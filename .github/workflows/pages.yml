# DEPRECATED — use static.yml only (P0 2026-09-13)
name: "[deprecated] pages.yml"
on:
  workflow_dispatch:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is deprecated. Canonical deploy: static.yml"
          exit 0
