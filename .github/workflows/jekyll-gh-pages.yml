# DEPRECATED — use static.yml only (P0 2026-09-13)
name: "[deprecated] jekyll-gh-pages"
on:
  workflow_dispatch:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated. Use static.yml for Vite → GitHub Pages."
