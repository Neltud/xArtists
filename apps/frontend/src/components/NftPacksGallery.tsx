/**
 * @deprecated — ne plus monter. Packs = uniquement page /agents (3 cartes Pulse·Yield·Sentinel).
 * Fichier conservé vide export pour éviter les imports morts.
 */
export default function NftPacksGallery() {
  return null
}
